# cpu_gpu
Small computer system for Altera Cyclon III FPGA development board that includes:
	- PS2 port
	- VGA port
	- SDRAM
Currenty there is an implementation of a text based GPU that uses the onboard SDRAM chip as video memory and uses VGA as output.
